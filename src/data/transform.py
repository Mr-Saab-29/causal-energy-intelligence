"""Data transformation utilities for raw energy records."""

from __future__ import annotations


def transform_energy_data(records: list[dict[str, object]]) -> list[dict[str, object]]:
    """Normalize raw energy records into an analytics-ready schema."""
    return records

